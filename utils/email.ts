export async function sendEmail(to: string, subject: string, body: string) {
  console.log(`Sending email to ${to}: ${subject}\n${body}`)
  // Implement actual email sending logic here (e.g., using an email service)
}

